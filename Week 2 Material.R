# & evalúa un vector completo && evalua sólo el primer elemento de un vector
# | evalúa un vector completo || evalua sólo el primer elemento de un vector

identical(7,7)
xor() #Es un or exclusivo, si un argumento es FALSE, entonces la función devolverá TRUE, en cualquier otro caso regresará FALSE

# sample() es una función que da una muestra aleatoria sin reemplazo

# which() toma un vector lógico como argumento y regresa los índices de los que son verdaderos

# any() regresa TRUE si uno o más de los elementos lógicos es verdadero
# all() regresa TRUE si sus elementos lógicos son verdadero

# difftime(Sys.time(), t1, units = 'days') 